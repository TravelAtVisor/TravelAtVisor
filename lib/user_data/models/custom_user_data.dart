class CustomUserData {
  final String nickname;
  final String fullName;
  final String? photoUrl;
  final String? biography;

  CustomUserData(this.nickname, this.fullName, this.photoUrl, this.biography);
}
